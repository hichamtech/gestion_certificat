{% extends 'layouts/adminLayout.html.twig' %}

{% block title %}SousModule{% endblock %}

{% block body %}
    <h1>SousModule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sous_module.id }}</td>
            </tr>
            <tr>
                <th>Libele</th>
                <td>{{ sous_module.libele }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sous_module_index') }}">back to list</a>

    <a href="{{ path('sous_module_edit', {'id': sous_module.id}) }}">edit</a>

    {{ include('admin/sous_module/_delete_form.html.twig') }}
{% endblock %}
