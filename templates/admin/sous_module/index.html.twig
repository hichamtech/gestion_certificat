{% extends 'layouts/adminLayout.html.twig' %}

{% block title %}Liste Sous Modules{% endblock %}

{% block body %}
    <section id="main">
        <div class="dashboard" id="dashboard">
            <div class="row">
                <div class="col-md-6">
                    <h1>Liste Sous Modules</h1>
                </div>
            </div>

            <div class="row mt-5">

                <div class="col-md-12">
                    <div class="card ">
                        <h4 class="card-title" style="text-align: center;font-weight: bold;background-color: rgb(33, 37, 41)">Liste</h4>
                        <div class="card-body align-items-center mt-3">
                            <div class="form-row mb-3">
                            </div>
                            <table class="table" style="margin-top:20px;">
                                <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Libele</th>
                                    <th>Module</th>
                                    <th>actions</th>

                                </tr>
                                </thead>
                                <tbody>
                                {% for sous_module in sous_modules %}

                                    <tr>
                                         <td>{{ sous_module.id }}</td>
                                         <td>{{ sous_module.libele }}</td>
                                         <td>{{ sous_module.module }}</td>
                                        <td>
                                            <div class="row">
                                                <div class="col-sm-2">
                                                    <a href="{{ path('sous_module_edit', {'id': sous_module.id}) }}" class="btn btn-warning btn-sm btn-icon icon-left text-light"> Éditer</a>
                                                </div>
                                                <div class="col-sm-2">
                                                    <a href="{{ path('sous_module_delete', {'id': sous_module.id}) }}" class="btn btn-danger btn-sm btn-icon icon-left text-light"> Supprimer</a>
                                                </div>

                                            </div>
                                        </td>
                                    </tr>

                                {% else %}
                                    <tr>
                                        <td colspan="7"> aucun enregistrement</td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                            <div class="row mt-3">
                                <div class="col-md-2">
                                    <a  class="btn btn-success " href="{{ path('sous_module_new') }}" >Nouveau</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

        </div>
    </section>
{% endblock %}