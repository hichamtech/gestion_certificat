{% extends 'layouts/adminLayout.html.twig' %}

{% block title %}TypeDemande{% endblock %}

{% block body %}
    <h1>TypeDemande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_demande.id }}</td>
            </tr>
            <tr>
                <th>Libele</th>
                <td>{{ type_demande.libele }}</td>
            </tr>
            <tr>
                <th>NombreMax</th>
                <td>{{ type_demande.nombreMax }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_demande_index') }}">back to list</a>

    <a href="{{ path('type_demande_edit', {'id': type_demande.id}) }}">edit</a>

    {{ include('admin/type_demande/_delete_form.html.twig') }}
{% endblock %}
